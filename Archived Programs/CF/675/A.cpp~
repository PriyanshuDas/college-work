#include <bits/stdc++.h>

#define sf scanf
#define pf printf
#define ll long long int
#define pb push_back
#define mp make_pair
#define ins insert

using namespace std;

int main()
{
	int a, b, c;	cin >> a >> b >> c;
	int diff = b-a;
	if(c == 0)
	{
		if(b == a)
			cout << "YES";
		else
			cout << "NO";
		return 0;
	}
	if(diff%c == 0)
	{
		int req = diff/c;
		if(req >= 0)
			cout << "YES";
		else
			cout << "NO";
		return 0;
	}
	cout << "NO";
	return 0;
}
