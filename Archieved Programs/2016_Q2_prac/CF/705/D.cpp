#include <bits/stdc++.h>

#define sf scanf
#define pf printf
#define ll long long int
#define mp make_pair
#define pb push_back;
#define N_max 5000

int N, s, e;
vector<int> X, J_s, J_b, L_s, L_b;
ll dist[N_max+1][N_max+1] = {0};

void dfs(int n)
{
    int flag = 0;
    for(int i = 0; i < N; i++)
    {
        if(visited[i] != -1)
        {

        }
    }
}
